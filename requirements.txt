# Pinned to the versions the platform is verified against (17 Jul 2026).
# Install:  pip install -r requirements.txt

# ---- Core web / API ----
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2

# ---- Persistence (SQLite by default — zero infra) ----
SQLAlchemy==2.0.51

# ---- Data / numerics ----
pandas==3.0.3
numpy==2.5.1

# ---- Market data / news ----
yfinance==1.5.1
httpx==0.28.1

# ---- Saxo streaming (WebSocket) ----
websockets==16.1

# ---- AI analysis layer (optional at runtime) ----
anthropic==0.116.0

# ---- Tooling / tests ----
pytest==9.1.1
python-dotenv==1.2.2
